
public class RequestSatisfier extends Thread {
	State state;
	
	State setRequest(Request request) {
		// if state.x > request then
		// 		state = new State(0,state.getQ)
		// set the request
		
	}
	
}
